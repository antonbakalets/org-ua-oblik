logging:
  level:
    root: WARN
    org.ua.oblik: INFO
    org.springframework: WARN

spring:
  datasource:
    url: jdbc:h2:mem:oblik_test
    username: sa
    password:

  flyway:
    baseline-on-migrate: true

  jpa:
    show-sql: true
